show_setup_info:
  name: Show setup info
  description: Show the generated webhook URL and the managed Protect sources.
  fields:
    entry_id:
      name: Config entry id
      description: Optional config entry id when more than one bridge is installed.
      required: false
      selector:
        text:

resync:
  name: Resync Protect bridge
  description: Refresh cameras and recreate managed Protect webhook automations when needed.
  fields:
    entry_id:
      name: Config entry id
      description: Optional config entry id when more than one bridge is installed.
      required: false
      selector:
        text:
